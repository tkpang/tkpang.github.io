# hello

A minimal "hello" landing page inspired by the iOS setup screen — a cursive
greeting that writes itself and cycles through languages.

- Single static `index.html`, no build step (`.nojekyll`).
- Light / dark via `prefers-color-scheme`, respects `prefers-reduced-motion`.
- Live at **https://hhhh.fun**
